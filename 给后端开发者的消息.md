# 给后端开发者的消息（中文版）

## 紧急：需要修复 Nginx 配置

Hi [后端开发者姓名],

我们的部署网站出现了一个紧急问题，需要在服务器上修复。

**问题：**
- 用户无法上传照片（创建帖子时）
- 错误：HTTP 413 (Request Entity Too Large)
- 服务器：18.166.68.164 (baicloud.miever.net)
- Nginx 版本：1.24.0 (Ubuntu)

**原因：**
Nginx 默认的请求大小限制（1MB）对于图片上传来说太小了。

**解决方案：**
请在服务器上 SSH 登录并运行以下命令：

```bash
sudo bash -c '
CONFIG_FILE="/etc/nginx/sites-available/default"
BACKUP="$CONFIG_FILE.backup.$(date +%Y%m%d-%H%M%S)"
sudo cp "$CONFIG_FILE" "$BACKUP"

# 在 server 块添加 client_max_body_size
if ! grep -q "client_max_body_size.*50M" "$CONFIG_FILE"; then
  sudo sed -i "/server[[:space:]]*{/a\    client_max_body_size 50M;" "$CONFIG_FILE"
  echo "✅ 已在 server 块添加 client_max_body_size 50M"
fi

# 在 location /api 块添加 client_max_body_size
if grep -q "location[[:space:]]*/api" "$CONFIG_FILE"; then
  if ! grep -A 10 "location[[:space:]]*/api" "$CONFIG_FILE" | grep -q "client_max_body_size.*50M"; then
    sudo sed -i "/location[[:space:]]*\/api[[:space:]]*{/a\        client_max_body_size 50M;" "$CONFIG_FILE"
    echo "✅ 已在 location /api 块添加 client_max_body_size 50M"
  fi
fi

# 测试并重载
if sudo nginx -t; then
  sudo systemctl reload nginx
  echo "✅ Nginx 配置已更新并重载成功"
  echo ""
  echo "验证："
  sudo nginx -T 2>/dev/null | grep "client_max_body_size"
else
  echo "❌ 配置测试失败，已恢复备份"
  sudo cp "$BACKUP" "$CONFIG_FILE"
  exit 1
fi
'
```

**或者手动编辑（如果你更喜欢）：**

1. 编辑 Nginx 配置文件：
   ```bash
   sudo nano /etc/nginx/sites-available/default
   ```

2. 找到 `server {` 块，在第一行添加：
   ```nginx
   server {
       client_max_body_size 50M;  # 添加这一行
       ...
   }
   ```

3. 找到 `location /api {` 块，在第一行添加：
   ```nginx
   location /api {
       client_max_body_size 50M;  # 添加这一行
       ...
   }
   ```

4. 保存文件（Ctrl+O, Enter, Ctrl+X）

5. 测试配置：
   ```bash
   sudo nginx -t
   ```

6. 如果测试通过，重载 Nginx：
   ```bash
   sudo systemctl reload nginx
   ```

**验证：**
修复后，运行以下命令验证：
```bash
sudo nginx -T | grep client_max_body_size
```

应该能看到至少两处 `client_max_body_size 50M;`

**说明：**
- 脚本会在修改前自动创建备份
- 如果配置测试失败，会自动恢复备份
- 这个修改只影响请求大小限制，不影响安全性

**修复后：**
修复完成后，用户就可以正常上传照片了，不会再出现 413 错误。

谢谢！

---

**如果需要 SSH 访问：**
- 服务器：ubuntu@18.166.68.164
- SSH 密钥应该在 GitHub Secrets 中配置好了（`SSH_PRIVATE_KEY`）

