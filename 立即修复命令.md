# 🚨 立即修复 HTTP 413 错误

## 当前状态
从错误信息看到仍然是 **HTTP 413 Request Entity Too Large**，说明 Nginx 配置还没修复。

## ✅ 最快的解决方案

### 方案 1：通过 GitHub Actions 立即运行修复（推荐）

由于单独的 fix-nginx-only.yml 工作流可能需要时间才能显示，我们可以直接在现有的 Development 工作流中添加一个快速修复步骤。

或者，**更简单的方法**：触发一个新的部署，因为部署流程中已经包含了自动修复步骤。

**在终端运行：**
```bash
cd /Users/yunbai/www-services
git commit --allow-empty -m "Trigger deployment to fix Nginx 413 error"
git push origin feature/test
```

这会立即触发一个新的部署，自动修复 Nginx。

---

### 方案 2：联系部署人员立即修复

把以下命令发给帮你部署的人，让他们在服务器上运行：

```bash
sudo bash -c '
CONFIG_FILE="/etc/nginx/sites-available/default"
BACKUP="$CONFIG_FILE.backup.$(date +%Y%m%d-%H%M%S)"
sudo cp "$CONFIG_FILE" "$BACKUP"

# 添加 server 块配置
if ! grep -q "client_max_body_size.*50M" "$CONFIG_FILE"; then
  sudo sed -i "/^[[:space:]]*server[[:space:]]*{/a\    client_max_body_size 50M;" "$CONFIG_FILE" || \
  sudo sed -i "/server[[:space:]]*{/a\    client_max_body_size 50M;" "$CONFIG_FILE"
  echo "✅ 添加了 server 块配置"
fi

# 添加 location /api 块配置
if grep -q "location[[:space:]]*/api" "$CONFIG_FILE"; then
  if ! grep -A 10 "location[[:space:]]*/api" "$CONFIG_FILE" | grep -q "client_max_body_size.*50M"; then
    sudo sed -i "/location[[:space:]]*\/api[[:space:]]*{/a\        client_max_body_size 50M;" "$CONFIG_FILE"
    echo "✅ 添加了 location /api 块配置"
  fi
fi

# 测试并重载
sudo nginx -t && sudo systemctl reload nginx && echo "✅ 修复完成！" || echo "❌ 配置错误，已恢复备份"
'
```

---

### 方案 3：检查当前部署状态

1. 打开：https://github.com/Miever1/www-services/actions
2. 查看最新的 Development 运行
3. 检查 "Fix Nginx config for large file uploads" 步骤是否成功
4. 如果有错误，告诉我具体的错误信息

---

## 🎯 推荐操作

**立即运行这个命令触发修复：**

```bash
cd /Users/yunbai/www-services && git commit --allow-empty -m "Fix: Trigger deployment to fix Nginx 413" && git push origin feature/test
```

然后等待 2-3 分钟，部署完成后测试上传功能。

