# 🚀 直接修复 Nginx - 两步搞定

## 步骤 1：点击这个链接

在浏览器中打开（直接复制粘贴到地址栏）：

```
https://github.com/Miever1/www-services/actions/workflows/fix-nginx-only.yml
```

## 步骤 2：点击 "Run workflow" 按钮

打开后你会看到：
- 右上角有一个绿色的 **"Run workflow"** 按钮
- 点击它
- 确保分支选择 `feature/test`
- 再点击绿色的 **"Run workflow"** 按钮

等待 1-2 分钟，完成后再测试上传照片！

---

## 📸 如果你看不到 "Run workflow" 按钮

可能的原因：
1. **工作流文件还没被识别**：等待 10-20 秒后刷新页面
2. **需要权限**：确保你是仓库的管理员或有写入权限

---

## 🔄 或者：等待自动修复

你的 "Development" 工作流（#37）正在运行，我已经在部署流程中添加了自动修复步骤，所以：
- 等待 #37 完成
- Nginx 配置应该会自动修复

---

## ✅ 验证修复

修复后，打开浏览器控制台（F12），再次尝试上传照片：
- 应该不再有 413 错误
- 应该可以成功上传

