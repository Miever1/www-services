# 修复照片上传失败问题（HTTP 413）

## 🎯 问题原因

你的后端是 **Deno**（使用 Hono 框架），部署在服务器上。上传照片失败是因为：

1. **Nginx 默认只允许 1MB 的请求**
2. 照片通常超过 1MB，导致 HTTP 413 (Content Too Large) 错误
3. 需要在服务器上的 Nginx 配置中增加 `client_max_body_size`

## ✅ 解决方案（已自动修复）

我已经修改了你的 **GitHub Actions 部署工作流** (`.github/workflows/development.yml`)，现在每次部署时会**自动修复 Nginx 配置**。

### 修复内容：
- 在 Nginx 配置的 `server {` 块中添加 `client_max_body_size 50M;`
- 在 `location /api {` 块中也添加 `client_max_body_size 50M;`
- 自动测试配置并重载 Nginx

## 🚀 如何使用

### 方法 1：推送代码触发自动修复（推荐）

1. **提交并推送代码**：
   ```bash
   git add .
   git commit -m "Fix: Add Nginx config for large file uploads"
   git push origin feature/test
   ```

2. **GitHub Actions 会自动运行**，在部署完成后自动修复 Nginx 配置

3. **等待部署完成**，然后测试上传照片

### 方法 2：手动在服务器上运行（如果需要立即修复）

如果你可以 SSH 到服务器，运行：

```bash
# 连接服务器（需要从 GitHub Secrets 获取 SSH 密钥）
ssh -i ~/.ssh/your-key ubuntu@18.166.68.164

# 运行修复命令
sudo bash -c '
CONFIG_FILE="/etc/nginx/sites-available/default"
sudo cp "$CONFIG_FILE" "$CONFIG_FILE.backup.$(date +%Y%m%d-%H%M%S)"
sudo sed -i "/^[[:space:]]*server[[:space:]]*{/a\    client_max_body_size 50M;" "$CONFIG_FILE"
sudo sed -i "/location[[:space:]]*\/api[[:space:]]*{/a\        client_max_body_size 50M;" "$CONFIG_FILE"
sudo nginx -t && sudo systemctl reload nginx && echo "✅ 修复完成！"
'
```

### 方法 3：联系部署人员

如果无法访问服务器，把以下信息发给帮你部署的人：

```
需要在服务器上修改 Nginx 配置以支持照片上传：

1. 编辑 /etc/nginx/sites-available/default
2. 在 server { 块的第一行添加：client_max_body_size 50M;
3. 在 location /api { 块的第一行也添加：client_max_body_size 50M;
4. 运行：sudo nginx -t && sudo systemctl reload nginx

或者运行：
sudo sed -i "/server[[:space:]]*{/a\    client_max_body_size 50M;" /etc/nginx/sites-available/default
sudo sed -i "/location[[:space:]]*\/api[[:space:]]*{/a\        client_max_body_size 50M;" /etc/nginx/sites-available/default
sudo nginx -t && sudo systemctl reload nginx
```

## 📋 技术细节

### 你的部署架构：
- **前端**：SvelteKit，部署到 `/home/ubuntu/www-service/frontend`
- **后端**：Deno + Hono，通过 PM2 运行在端口 8000
- **数据库**：PostgreSQL
- **反向代理**：Nginx（需要修复的地方）
- **部署方式**：GitHub Actions → SFTP → 服务器

### 修复原理：
Nginx 作为反向代理，在请求到达 Deno 后端之前会检查请求大小。默认限制是 1MB，我们需要增加到 50MB 来支持照片上传。

## ✅ 验证修复

修复后，测试步骤：

1. 访问：https://baicloud.miever.net/post
2. 尝试上传一张照片（可以是大一点的图片）
3. 检查浏览器控制台（F12），应该不再有 413 错误
4. 应该能看到"任务创建成功"的消息

## 🆘 如果还是不行

检查以下几点：

1. **确认部署已运行**：
   - 访问：https://github.com/Miever1/www-services/actions
   - 查看最新的部署是否成功

2. **查看服务器日志**：
   ```bash
   # Nginx 错误日志
   sudo tail -f /var/log/nginx/error.log
   
   # Deno 后端日志（通过 PM2）
   pm2 logs www-backend
   ```

3. **确认 Nginx 配置已更新**：
   ```bash
   sudo nginx -T | grep client_max_body_size
   # 应该看到两处 50M
   ```

## 💡 后续优化建议

如果照片还是太大，可以考虑：

1. **客户端压缩**：在上传前压缩照片
2. **使用图片 CDN**：将图片上传到云存储（如 AWS S3、阿里云 OSS）
3. **分块上传**：对于超大文件使用分块上传

但目前 50MB 的限制应该足够日常使用了。

